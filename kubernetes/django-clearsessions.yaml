apiVersion: batch/v1
kind: CronJob
metadata:
  name: clearsessions
spec:
  schedule: "0 0 1 * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: job
              image: django_app:latest
              imagePullPolicy: IfNotPresent
              command: ["./manage.py", "clearsessions"]
              envFrom:
                - secretRef:
                    name: secret
